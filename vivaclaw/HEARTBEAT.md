# VivaClaw Heartbeat

- Check if any `/tmp/vivaclaw_post_route.dcp` is newer than `memory/last_run.md` — if so, note that a run completed but metrics may not be recorded.
- Check `memory/design_notes.md` exists — create if missing.
